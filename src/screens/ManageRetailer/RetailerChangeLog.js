import React from 'react';
import { Alert } from 'react-bootstrap';

const RetailerChangeLog = () => {
    return (
        <Alert variant='secondary'>Change Log</Alert>
    );
}

export default RetailerChangeLog;
